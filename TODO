- bovnar spec version in files
- richer string escapes (arbitrary code points and bytes with  `\u`, `\x`)
- native time family
- Reference path indexing for arrays (e.g. : .matrix = [10, 20, 30]/[40, 50, 60]; .row0c1 = &.matrix[0][1];          # → 20 ) -> reference resolution at DOM layer only